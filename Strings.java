public class Strings {
    public static void main (String[] args){
// Strings are reference types in Java but in the shorter mode, we can assign it just like the primitive type
        String str = new String("Hello");
//        There are various methods present with the Strings. Strings are immutable in Java.

//        There are some escape chars we use: \", \\, \n, \t
    }
}
